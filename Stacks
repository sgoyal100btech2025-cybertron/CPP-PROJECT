/*
#include<iostream> 
using namespace std;

class stack{
int s[10],y;
public:int topI =-1;
    void push( int val){
      topI++;
      s[topI]=val;
      }  
   int pop()  {
      y=s[topI];
      topI--;
    //  return y;
  }
  int top(){
    return s[topI];}
    bool empty(){
     return topI ==-1;
   
  }};
  
int main()  {
  
  stack s;
  
  s.push(10);
  s.push(20);
  s.push(30);
  s.pop();
  s.push(40);
  
  
  while(!s.empty()){
    cout<<s.top()<<" ";
   s.pop();    
  }  
}*/
 
#include<iostream> 
#include<stack>
using namespace std;


int main()  {
  
  stack <int>s;
  
  s.push(10);
  s.push(20);
  s.push(30);
  s.pop();
  s.push(40);
  
  
  while(!s.empty()){
    cout<<s.top()<<" ";
   s.pop();    
  }  
}